# OTP formalisation ledger — implemented vs remaining, toward FULL formalisation beyond the papers

*2026-07-17. Fine-grained status of the OTP process-algebra metatheory formalised in Cure.
"Beyond the papers" baseline: **NVLang** (typing plumbing, proof sketches, toy single-actor),
**Special Delivery / Proof of Delivery** (mailbox types, commutative, operational preservation
for the type language DEFERRED), **de'Liguoro–Padovani** (commutative-regex mailbox, paper
proofs), **KWC** (ordered signals + monitors, no types). Our delta: **mechanized, dependently +
linearly typed, operational preservation over the REAL reduction, tied to inference.***

---

## PART A — IMPLEMENTED (shipped in `lib/std/otp_*.cure`, each Idris `rel=same`)

### A1. Concurrent reduction + preservation / progress
- [x] Ether→mailbox concurrent reduction (Core Erlang, Bereczky–Horpácsi–Thompson 2311.10482) — `otp_preservation.cure`
- [x] Subject reduction (tag safety) over that real reduction — `preservation` *(beyond NVLang: its preservation is over a toy step)*
- [x] Progress + type safety: a well-typed config is final or steps to a well-typed config, never stuck — `otp_safety.cure` *(NVLang has no progress theorem)*
- [x] Multi-pid INTERLEAVING preservation (async; any one process steps, others fixed) — `otp_system.cure` *(beyond NVLang's single-actor step)*
- [x] Mailbox FIFO order (arrive=snoc/end, recv=front) + `AllAccepted`-over-append lemma — `otp_fifo.cure`

### A2. Obligation 1 — per-constructor HETEROGENEOUS + LINEAR dependent reply
- [x] `ReplyOf(req)` via large elimination (per-request reply type) — `otp_proof.cure`
- [x] Linear reply capability consumed EXACTLY ONCE (QTT grade + usage; drop/dup/one-branch-drop/launder all reject) — `otp_proof.cure`
- [x] Branching reply handler (per-branch reply), drop/dup rejected — oracle `ob1_branching*`
- [x] Reply preservation through delivery = COMPOSE G1×G2 (`HasReply(r,v)`, `reify` bridge) — `otp_reply_preservation.cure`
- [x] Reply CONSERVATION, operational exactly-once (drain: complete run `(n,Z)→(Z,m) ⟹ n≡m`) — `otp_reply_conservation.cure`

### A3. Obligation 2 — clause-DERIVED pid message index
- [x] `Pid(m)` carries its handler; index derived via `spawn_actor`; `post` dispatches — `otp_proof.cure`
- [x] Effect-honest `spawn_actor`/`post` returning `Effect(...)`, threaded by `effect_bind` — `otp_send_effect.cure` *(NVLang explicitly excludes effects §8.4)*

### A4. Monitors / links / signals (G3)
- [x] Typed monitor / DOWN-as-message (`MRef` carries `Accepted(TDown)`) — `otp_monitor.cure` *(NVLang: MonitorRef in grammar only)*
- [x] Typed links + `trap_exit` dispatch (message-vs-death on the flag) — `otp_link.cure`
- [x] Demonitor (`MRef` Active/Removed; DOWN-after-demonitor unconstructible) — `otp_demonitor.cure`
- [x] Unlink (`LinkRef` Linked/Unlinked; no exit signal after unlink, keeping trap dispatch) — `otp_unlink.cure`
- [x] Reason-dependent exit propagation (`Step` indexed by `Normal`/`Abnormal`/`Kill`; normal doesn't kill a non-trapper, kill is untrappable) — `otp_exit_signal.cure`

### A5. Timers (G4) — *no paper types timers*
- [x] `send_after` typed; `TimerRef` Pending/Cancelled; fire-after-cancel unconstructible; `receive after` branch — `otp_timer.cure`

### A6. `gen_server:call` failure/totality (G6) — *no paper types the timeout*
- [x] `CallOutcome(r) = Replied(r) | Failed(CallError)`; consumer ignoring `Failed` rejected non-total — `otp_call.cure`

### A7. Registry / naming (G7) — conformance F-2/F-2c
- [x] `GenServer(q,r)` wraps a `Pid` (bare `Pid` ≠ `GenServer`); `whereis → PidOption` partiality; ignoring `NoPid` rejected — `otp_registry.cure`

### A8. Selective receive (G8 slice)
- [x] ORDERED selective receive (front-to-back scan; order = inductive structure); `preserves` + `received_accepted` — `otp_selective_receive.cure` *(mailbox papers name ordered receive as future work)*

### A9. Routing / inter-process communication
- [x] Cross-process delivery (homogeneous), routing preserves global WT — `otp_routing.cure`
- [x] HETEROGENEOUS routing (per-process interfaces via `Member`/`AllMember`) — `otp_het_routing.cure`

### A10. Supervision (C4)
- [x] `Fleet(specs)` indexed by spec list; `restart_all`/`restart_one` return same specs (type certifies restart preserves declared children) — `otp_supervisor.cure` *(NVLang never ties restart to preservation)*

### A11. Mailbox type INFERENCE (G9) — *the universally-named open problem (OTP #5364)*
- [x] Decidable core: `tag_eq`, `decide_handles`, `decide_all_handled` (total, proof-carrying) — `otp_inference.cure`
- [x] Inference LAWS: monotonicity, weakening/subtyping-transitivity, principality (`self_member`) — `otp_inference_laws.cure`
- [x] ADEQUACY, sequential + branching (`BNil`/`BRecv`/`BSend`/`BSeq`): `preservation_at`, `coverage`, `adequacy` over runs — `otp_inference_adequacy.cure` *(the operational half Proof of Delivery deferred, tied to inference)*
- [x] Pre-fixpoint bound (`lfp_le`, principality direction of Knaster–Tarski) via monotone Kleene iteration — **SHIPPED** `otp_inference_fixpoint.cure` (Idris `rel=same`)

### A12. Cross-cutting
- [x] Exhaustiveness / dead-letter prevention (C8): kernel-certified totality + exhaustiveness on every proof term
- [x] 19/22 modules pure intrinsic (indexed types + total match, no `reflexive`/`rewrite`/`with`); 35 Idris oracle pairs `rel=same`

---

## PART B — REMAINING (fine-grained), toward FULL formalisation

### B1. Near-term, in-Cure provable (small or elaborator-gated)
- [x] `unlink` (demonitor-shape over `Link`, keeping the trap dispatch) — SHIPPED `otp_unlink.cure`
- [x] EXIT **reason axis** — reason-dependent dispatch (`normal`/`abnormal`/`kill`) in `otp_exit_signal.cure`. Still open: a full reason *term* as payload, and monitor-ref correlation on DOWN
- [x] Cascading exit-signal propagation across a link CHAIN — `otp_cascade_exit.cure` (`Cascade` relation + `run_cascade` totality: terminates, survivors = suffix from first trapper). Open: a general link GRAPH (fan-out) vs the chain slice
- [ ] **E1** sibling-context refinement on `match` (elaborator ergonomics — removes the data-first tax) — spec `2026-07-17-proof-authoring-elaborator-ergonomics-design.md`
- [ ] **E3 / task #15** cross-module resolution of implicit-carrying stdlib fns (lets proof lemmas factor across modules)
- [ ] **E4** partial-app codegen (eta-expand explicit-arg partial applications)

### B2. Recursion fixpoint — genuine in-Cure research (Knaster–Tarski / Kleene)
- [x] `BRec` behaviour representation (`RBody` with single recursion variable `RVar`) + syntax-derived transfer `tset(body, I)` + `infer(BRec) = rec_infer` — **SHIPPED** `otp_recursive_transfer.cure` (Idris `rel=same`)
- [x] transfer MONOTONICITY generic over syntax (`tset_mono`, induction on the body; `orb_mono`/`join_mono`/`setbit_mono`) — the hypothesis `map_lfp_le` needs — **SHIPPED** `otp_recursive_transfer.cure`
- [x] fixed-point property at a syntactic transfer (`rec_fixed = map_lfp_le(tset(body,-), tset_mono(body))`) — **SHIPPED** `otp_recursive_transfer.cure`
- [x] `lfp` construction via bounded Kleene iterate `iterate(f, ⊥, height)` — **SHIPPED** (`rec_infer = iter(tset(body,-), 4)`)
- [x] `?lfp_le` pre-fixpoint bound — **SHIPPED** `otp_inference_fixpoint.cure`
- [ ] `?le_lfp` (lfp below any upper bound of pre-fixed points)
- [x] **STABILIZATION**: `iterate(f, ⊥, |Tag|)` IS a fixed point — finite-height counting (interface size ≤ 3, strict-increase-or-fixed) — **SHIPPED** `otp_finite_fixpoint.cure` over the height-3 `IF` lattice (Idris `rel=same`). *The hard half, mathlib `monotone_chain_condition` port.*
- [x] `?map_lfp` (fixed-point property `f(lfp) ⊑ lfp`) — **SHIPPED** `map_lfp_le` in `otp_finite_fixpoint.cure`
- [x] `IF ↔ TagList` bridge (`denote`, `denote_complete`, `sub_allhandled`) — **SHIPPED** `otp_interface_bridge.cure` (Idris `rel=same`); transfers `map_lfp_le`'s `Sub` conclusion to `AllHandled` membership, so the finite fixpoint is usable in the adequacy representation
- [x] `?is_least` principality — `lfp_le` (Kleene pre-fixpoint bound over the `IF` lattice) + `rec_is_least` (the inferred interface is contained in every self-consistent interface; with `rec_fixed` it is the LEAST fixed point) — **SHIPPED** `otp_recursive_transfer.cure` (Idris `rel=same`)
- [x] BRec **coverage** — every direct send tag of a recursive behaviour is a member of its inferred fixed-point interface (`RSendsIn`, `tset_covers`, `brec_covers`), in both bit-set and membership (`brec_handles : Handles(t, denote(rec_infer(body)))`) form — **SHIPPED** `otp_recursive_adequacy.cure` (Idris `rel=same` on the `tset_covers` induction; the membership bridge reuses the oracle-verified `denote_complete` + `rec_fixed`). Canonical `Tag` now shared across `InterfaceBridge`/`RecursiveTransfer`/`RecursiveAdequacy`.
- [x] BRec **adequacy** proper — every config reachable by running a recursive behaviour is well-typed at `denote(rec_infer(body))` (`preservation_at` + `adequacy_at` + `rec_send_step` + `rec_adequacy`) — **SHIPPED** `otp_recursive_run.cure` (Idris `rel=same` on the operational core; `rec_send_step` justifies recursive sends via `brec_handles`). BRec inference is now operationally sound end to end for the finite universe: coverage (fundamental theorem) + preservation (operational closure).
- Scaffold: `scaffolds/inference_fixpoint.cure`; shape doc `2026-07-17-mailbox-inference-fixpoint-shape.md`

### B3. Counting / multiplicity fragment — commutative-regex mailbox types (Special Delivery, de'Liguoro–Padovani)
*(the tag-SET model does not reach this; it is a set, not a multiset with counts)*
- [x] Mailbox types as COMMUTATIVE REGEX (patterns `PZero`/`POne`/`PAtom`/`PPlus`/`PTimes`/`PStar`) over MULTISETS (Parikh vectors `MkMS(Nat,Nat,Nat)`), with the `Accepts` relation and the COMMUTATIVE-MONOID laws of `(Pat, PTimes, POne)`: `times_comm` (`E·F ≡ F·E`), `times_assoc`, `one_times` (unit), plus `plus_comm` (choice) — all via `msadd` arithmetic (`msadd_comm`/`msadd_assoc`/`msadd_zero_left` over Nat `plus_comm`/`plus_assoc`) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). The counting model the tag-set inference cannot express.
- [~] Brzozowski derivatives for pattern matching: `nullable` + `nullable_sound`; the commutative `deriv(E,t)` and **`deriv_sound`** — the SOUNDNESS half of the fundamental theorem, `Accepts(deriv(E,t), m) → Accepts(E, m ⊎ {t})` — proved for the full pattern algebra incl. `PStar` (constructive; relocates the peeled `{t}` with the bag-algebra laws) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). REMAINING: the CONVERSE (`deriv_complete`) and `nullable_complete` both need `msadd` zero/one INVERSION, blocked by a Cure elaborator gap (stuck-index equation not retained on GADT match; and sequential-match refinement — see below).
  - **Elaborator parity gaps found (cataloged for the ergonomics spec):** (1) matching an `ATimes`-style ctor whose index is a stuck function-app (`msadd(m1,m2)`) does not retain the index equation `m = msadd(m1,m2)` as a usable proof — blocks any inversion. (2) SEQUENTIAL-MATCH refinement: `match pat` then a separate `match acc → … → ATimes` does not propagate the second match's index refinement into the goal the first match already specialized (Idris matches all patterns simultaneously per clause; Cure's `match` is independent per scrutinee). WORKAROUND for (2): helper-delegation — match the evidence as a function PARAMETER (no prior data-match freezing the goal). Used in `deriv_sound`'s `ds_times`/`ds_star`.
- [x] KLEENE STAR FIXPOINT LAW `*E ≡ 𝟙 ⊕ E·(*E)` (`star_unfold`/`star_fold`, both directions, pure constructor rearrangement) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] SEMIRING left-DISTRIBUTIVITY `E·(F⊕G) ≡ (E·F)⊕(E·G)` (`dist_fwd`/`dist_bwd`, both directions) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] ADDITIVE monoid + ANNIHILATOR: `⊕` associative (`plus_assoc_fwd`/`bwd`) with unit `PZero` (`plus_zero_fwd`/`bwd`), and `PZero` absorbs `·` (`zero_times`) — `(Pat, ⊕, 𝟎, ·, 𝟙, *)` is a commutative KLEENE ALGEBRA up to acceptance — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] Pattern SUBTYPING (inclusion): inductive `Incl(E,F)` (refl, `𝟎`-least, choice lub/injections, `·`/`*` monotone, transitive) + `incl_sound` (implies semantic inclusion — a mailbox typed `E` is usable where `F` is expected) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). Note: this is the SOUND syntactic subtyping; the DECISION procedure for semantic inclusion needs the Presburger solver (out of TCB).
- [x] Derivative MONOTONICITY under subtyping (`deriv_mono : Incl(E,F) → Incl(deriv(E,t), deriv(F,t))`, pure `Incl` recursion) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`).
- [x] BRZOZOWSKI MATCHING (operational membership decision): `dfold` folds the derivative along a message `Word`, matching = residual `nullable`; `matches_word_sound` proves it sound (`nullable(dfold(E,w))=T ⟹ Accepts(E, parikh(w))`) — **SHIPPED** `otp_mailbox_pattern.cure` (Idris `rel=same`). The operational use of the derivative; completeness is E9-blocked.
- [ ] Conformance (`reliable`/`usable`, de'Liguoro–Padovani Def. 10)
- [ ] Bidirectional constraint GENERATION from a behaviour
- [ ] **Presburger / semilinear (Parikh) SOLVING** — external Z3/omega backend, **OUT OF TCB** (untrusted lint per the SMT trust-boundary decision), not a kernel proof
- [~] Session-types-into-mailboxes encoding (de'Liguoro–Padovani §4.3–4.4): binary session types + DUALITY foundation — `dual`, `dual_involution` (dual is an involution), `Compat` + `compat_dual` (compatible endpoints are exactly dual endpoints, i.e. communication safety = duality) + `dual_compat` (the CONVERSE: every type IS compatible with its dual, `Compat(S, dual(S))` — closing the "compatible ⟺ dual" equivalence in BOTH directions) + `compat_sym` (compatibility is SYMMETRIC: `Compat(l,r) ⟹ Compat(r,l)`, the two endpoints are interchangeable) + `compat_unique` (compatible peer is UNIQUE: `Compat(l,r)` and `Compat(l,r2)` ⟹ `r = r2` — both equal `dual(l)` and `dual` is injective, so a well-typed channel determines its other endpoint's type unambiguously) — **SHIPPED** `otp_session.cure` (Idris `rel=same`). [Attempted but DEFERRED: the mailbox-encoding monoid-homomorphism `recvs(seq(s,t)) = msum(recvs s, recvs t)` — blocked by a KERNEL normalizer incompleteness (conversion won't reduce a reducible arg in a stuck application's spine, e.g. `recvs(LEnd)` inside a stuck `msum`); recorded as finding **K1** in the ergonomics spec, TCB HARD-STOP.] plus `SStep` + `session_preservation` (subject reduction: a communication step between compatible endpoints yields compatible endpoints — a safe session stays safe as it runs). Plus `SRun` + `session_run_safe` (safety over a WHOLE run: a session from compatible endpoints stays compatible through every step — never gets stuck on a type mismatch). Plus `Progress` + `session_progress` (PROGRESS: a compatible pair is either finished (`SEnd`/`SEnd`) or a communication redex — a well-typed session never deadlocks; with preservation this is full type safety, well-typed ⟹ never stuck). Plus `sstep_sym`/`srun_sym` (STEPS AND RUNS REVERSE UNDER ENDPOINT SWAP: `SStep(l,r,l2,r2) ⟹ SStep(r,l,r2,l2)` and likewise for `SRun` — send↔receive, select↔offer; the operational face of `compat_sym`, so the two participants' views of a session's execution are mirror images). Plus `compat_terminates` (NORMALISATION: every compatible finite session runs all the way to `(SEnd, SEnd)` — an explicit full `SRun` built by structural induction; packages progress + preservation + termination, so a well-typed session never deadlocks, never mistypes, and always terminates). The construction uses the explicit-`tail` pattern to pin `SRStep`'s floating target indices past the E6-residual construction-position gap (documented in the ergonomics spec). Plus `compat_terminates_len` (TERMINATION TIME IS EXACT: `srun_len(compat_terminates(c)) = sdepth(l)` — the canonical terminating run takes EXACTLY `sdepth(l)` communication steps, sharpening normalisation from "reaches completion" into a precise step count fixed by the endpoint's type depth; induction on the compatibility derivation, one step per send/receive/chosen-branch via `snat_cong`). Plus RECURSIVE session types (`Std.Otp.RecursiveSession`): `RSType` with `μX`/`RVar`, `subst`/`unfold`/`rdual`; `rdual_involution`, `subst_dual` (dual distributes over substitution), and the headline `dual_unfold_commute` (`dual(unfold(μX.S)) = unfold(μX.dual(S))` — a looping protocol's endpoints stay dual across every unfolding); generalized to `rdual_unfold` (`dual(unfold(S)) = unfold(dual(S))` for EVERY shape, of which `dual_unfold_commute` is the `RMu` instance). Plus MULTIPARTY session types (`Std.Otp.MultipartySession`): `Global` protocol type + `project(G, role)` to per-role `Local` types; `projection_duality` proves COHERENCE (a two-party global projects to DUAL endpoints — global well-formedness yields local communication safety, Honda–Yoshida–Carbone). Plus `GStep` + `twoparty_preserved` (GLOBAL SUBJECT REDUCTION: firing the head interaction of a well-formed two-party protocol yields a well-formed two-party protocol — so with `projection_duality` the projected endpoints stay coherent through every step of the protocol's execution). Plus `GProgress` + `global_progress` (GLOBAL PROGRESS: a well-formed two-party protocol is either finished or its head can fire — never deadlocks; with `twoparty_preserved` this is full safety for the global protocol). Plus OPERATIONAL CORRESPONDENCE (`LStep` + `proj_sender_steps`/`proj_receiver_steps`): when the global fires its head `RA → RB : t`, the SENDER's projection takes a local SEND step and the RECEIVER's a local RECEIVE step, both to the continuation's projection — so projection is sound w.r.t. reduction (global steps are mirrored by matching local steps at each participant, not merely statically coherent). Plus `grun_preserves` (GLOBAL SAFETY OVER A RUN: `TwoParty(g)` + `GRun(g,g2)` ⟹ `TwoParty(g2)` — a protocol started well-formed stays well-formed through EVERY step, so with `projection_duality` the projections stay coherent at every reachable point; multi-step strengthening of `twoparty_preserved`). Plus `GRun` + `twoparty_terminates` (GLOBAL NORMALISATION: every well-formed two-party protocol runs all the way to `GEnd` — a full `GRun` built by structural induction firing the head at each step; with progress + subject reduction, the global protocol never deadlocks, stays well-formed, and always terminates; uses the explicit-`tail` construction pattern). Plus the SESSION→MAILBOX ENCODING (`Std.Otp.SessionMailbox`, the de'Liguoro–Padovani headline): an endpoint is realized as a process with a MAILBOX = the commutative multiset (`MS`) of tags it receives; `recvs`/`sends` measure the received/emitted multisets, and `recvs_dual` proves ENCODING FIDELITY — `recvs(dual(L)) = sends(L)`, the mailbox an endpoint must service is exactly what its dual peer sends (with the mirror `sends_dual`), so the encoding preserves communication safety. Plus FORK/JOIN (parallel session composition `LPar`): an endpoint may run two independent sub-sessions in parallel, whose mailbox is the multiset SUM (`msum`) of the components'; `recvs`/`sends`/`dual` extend componentwise and fidelity (`recvs_dual`/`sends_dual`) still holds via `msum_cong` — so parallel composition preserves the encoding's communication safety. Plus ENCODING BALANCES COMPATIBILITY (`Compat` over `Local` + `compat_recv_send`/`compat_send_recv`): for compatible endpoints one side's mailbox multiset is exactly the other's emissions (`recvs(l) = sends(r)` and `sends(l) = recvs(r)`, fork/join included) — the encoding turns a well-typed (compatible) session into a pair of mailboxes that exactly service each other's traffic, communication safety at the mailbox level. Plus THE ENCODING IS A MONOID HOMOMORPHISM (`seq` + `recvs_hom`/`sends_hom`): sequential composition of sessions maps to multiset SUM of mailboxes — `recvs(seq(s,t)) = msum(recvs(s), recvs(t))` (and `sends` likewise), so the mailbox of a composite protocol is exactly the union of its parts'. **Unblocked by the K1 KERNEL FIX** (`fix(core)` `dee86c0b`: `Normalise.reduce_unfolded`'s A6 freeze now fires consistently, so `msum(recvs(LEnd), …)` and `msum(empty, …)` compare equal); supporting algebra `add_assoc`/`msum_assoc`/`bump_msum_dist`/`msum_empty_l`. Plus the FULL ALGEBRAIC STRUCTURE: `(MS, msum, empty)` is a COMMUTATIVE MONOID (`msum_comm` via `add_comm`, `msum_assoc`, two-sided unit `msum_empty_l`/`msum_empty_r`) and `(Local, seq, LEnd)` is a MONOID (`seq_assoc`, two-sided unit `seq(LEnd,s)=s` definitional + `seq_end_r`) — so with `recvs_hom`/`sends_hom` preserving the unit (`recvs(LEnd)=empty`), `recvs`/`sends` are genuine MONOID HOMOMORPHISMS from the session monoid to the commutative mailbox monoid (Nat lemmas `add_zero_r`/`add_succ_r`/`add_comm`). Plus MAILBOX TYPES FORGET ORDER (the point of the encoding): `recvs_par_comm` (fork/join is order-insensitive, `recvs(LPar(a,b))=recvs(LPar(b,a))`) and `recvs_seq_comm`/`sends_seq_comm` (`recvs(seq(a,b))=recvs(seq(b,a))` even though `seq` is NOT commutative — the mailbox is a commutative BAG, so swapping two composed sub-protocols leaves the expected-message multiset unchanged). This DERIVES why mailbox types can be commutative, from the monoid homomorphism + `msum_comm`. Plus COMPOSITIONALITY (`mailbox_seq_cong`/`mailbox_par_cong`): substituting mailbox-EQUIVALENT sub-protocols (`recvs(s)=recvs(s2)`) leaves the composite mailbox unchanged — the mailbox of a protocol depends only on its parts' mailboxes, not their internal structure, so sub-protocols can be type-checked independently and composed (modularity of mailbox typing). Plus THREE-ROLE PROJECTION (`Std.Otp.ThreeParty`, the tractable slice of >2-role coherence — full N-role coherence needs branch-MERGE, deferred): three roles `RA`/`RB`/`RC`; `bystander_ab` proves a role party to none of a bilateral protocol's messages projects to `LEnd` (NON-PARTICIPATION — the third role can be spawned or omitted freely; the message-skip discharges definitionally since `RA`/`RB` ≠ `RC`), and `bilateral_duality` proves an `RA`/`RB`-bilateral protocol projects those two to DUAL endpoints unaffected by `RC`'s presence (projection is LOCAL). Plus SESSION SUBTYPING (`Std.Otp.SessionSubtyping`, Gay–Hole safe substitutability): `Sub(s,t)` = an endpoint typed `s` is usable where `t` is expected. The crux is OPPOSITE width variance of the two choice forms — internal choice `SSelect` (⊕) is COVARIANT in its label set (a subtype offers FEWER labels, `s1 ⊆ s2`), external choice `SBranch` (&) is CONTRAVARIANT (offers MORE, `s2 ⊆ s1`); send/receive continuations covariant. Label sets are pointwise boolean orders (`LSub` over `BLe`) so the ⊆ lemmas are clean inductions not boolean case-bashing. Proven: `sub_refl` + `sub_trans` + `sub_antisym` (Sub is a PARTIAL ORDER — mutual subtypes are structurally equal, via `ble_antisym`/`lsub_antisym` down through the boolean-order and label-set layers; the external-choice case composes the label witnesses in FLIPPED order for its contravariance) and `sub_dual_antitone` (duality REVERSES subtyping — `dual` swaps ⊕/& so the choice cases exchange constructors, making subtyping sound for both channel endpoints). Plus the SOUNDNESS capstone `narrow` (SUBSTITUTABILITY: `Sub(s,s2)` and `Compat(s2,t)` ⟹ `Compat(s,t)` — a subtype can be safely substituted in any compatible session; each choice case composes the label witnesses with `lsub_trans` in its variance-dictated order, `s ⊆ s2 ⊆ t` for select, `t ⊆ s2 ⊆ s` for branch) — the theorem that gives subtyping its meaning. Choices carry a shared continuation (faithful simplification of per-label continuations); n-ary per-label branching deferred. Plus SESSION DELEGATION / HIGHER-ORDER CHANNELS (`Std.Otp.SessionDelegation`): `SDeleg(a, k)` sends a channel of session type `a` then continues as `k`, `SResume(a, k)` receives it — the distinguishing higher-order session feature (e.g. a balancer handing a live connection to a worker). KEY subtlety: the delegated payload `a` is transferred AS-IS (NOT dualized — the receiver takes over the exact endpoint); only the carrier continuation is dualized. `dual_involution` still holds (carried `a` copied verbatim through both duals) and `compat_dual` still holds (delegate meets resume on the same `a`), so communication safety = duality even with first-class channels. Plus the SESSION↔OTP-CALL BRIDGE (`Std.Otp.CallSession`): `gen_server:call` as a DEPENDENT binary session whose reply tag is COMPUTED from the request (`reply_for(req)`, the session-typed face of `ReplyOf(req)`); `client(req)`/`server(req)` are the endpoints, `call_duality` proves `client(req) = dual(server(req))` for EVERY request by reflexivity (uniform dependent well-formedness — `dual` never inspects the stuck `reply_for(req)`), and `call_compat` proves the dependent request/reply exchange is compatible for all requests. Plus the VALUE-LEVEL dependent call result (`Std.Otp.CallResult`, G6): a call returns `Ok` carrying a reply whose VALUE TYPE is computed from the request via LARGE ELIMINATION `RepVal(reply_for(req))` (RGet↦Nat, RPut↦Bit) or `Err(CallError)` (Timeout/NoProc/ServerDied); `get_reply`/`put_reply` witness that the success payload really has the request-determined type (a `Bit` where a `Nat` is required is rejected), and `describe` is total only because it handles both outcomes — a consumer that ignores failure is rejected non-total, so a failed call can't be silently dropped. This is the value-level face of `ReplyOf(req)`. Plus THE BRANCH-MERGE OPERATOR + CHOICE PROJECTION COHERENCE (`Std.Otp.BranchMerge`, the deferred N-role machinery): global protocols with CHOICE (`GCho(from,to,tL,gL,tR,gR)`) and local types with internal/external choice (`LSel`/`LBra`). The `merge` operator — TOTAL, `LErr` on conflict — is the crux of multiparty projection: projecting onto a role that's a BYSTANDER to a choice must combine its two branch-behaviours, and the defining rule is that DIFFERING RECEIVES UNION into an external choice (`merge(LRecv(a,·), LRecv(b,·)) = LBra(a,·,b,·)` for `a≠b` — the bystander offers both labels since it learns which branch was taken by which label it receives), while differing SENDS conflict (`LErr`). `merge_idem` proves `merge(L,L)=L` (a type merges with itself — the identical-bystander base of coherence). The headline `choice_duality` proves `Coherent(g) ⟹ project(g,RA) = dual(project(g,RB))` — a coherent RA/RB protocol WITH CHOICE projects its two active roles to DUAL `LSel`/`LBra` endpoints (`dual` swaps internal⊕external choice), so `Std.Otp.ThreeParty`'s `bilateral_duality` is now LIFTED to branching protocols, in a system carrying the branch-merge operator. FINDING: the total-merge-with-`LErr` design was essential — an initial partial `LOpt` projection forced the coherence proof to name the matched `Coherent` constructor's existential index `k` (which is UNnameable in source: `:unknown_global`; and making the continuation implicit erases it, so it can't be matched: `:erased_used_relevantly`). Making `merge`/`project` total (so `dual` reduces directly on `Local`) let `choice_duality` go through by plain constructor congruence exactly like `bilateral_duality`, no index-naming. **SHIPPED** `otp_branch_merge.cure` (Idris `rel=same`). Plus N-ROLE BYSTANDER PROJECTABILITY (the branch-merge CONSUMED in a coherence proof): the MERGEABILITY relation `Mergeable(x,y)` — the standard multiparty notion characterizing exactly when two bystander branch-behaviours combine (same sends / same-tag receives merge pointwise; DIFFERING receives `a≠b` union via a `tag_eq(a,b)=F` witness; choices merge on matching labels) — with `merge_ok` (`Mergeable(x,y) ⟹ merge(x,y) ≠ LErr`, i.e. mergeable types have a DEFINED merge; the differing-receive case cased 9-way so `tag_eq` computes and the impossible diagonal refutes the `a≠b` witness). Then the headline `bystander_defined` (`WF(g) ⟹ is_err(project(g, RC)) = F`): a WELL-FORMED global (predicate `WF` with genuine bystander RC receiving from RA/RB, and a `Mergeable` witness carried at each choice — the multiparty well-formedness SIDE CONDITION) projects the bystander to a DEFINED local type, never a conflict. The CHOICE case is `project(g,RC) = merge(project(gL,RC), project(gR,RC))`, discharged by `merge_ok(mg)` — so the branch-merge operator is CONSUMED in the coherence proof and multiparty projection is TOTAL on well-formed protocols. So both multiparty coherence facts now hold WITH CHOICE: active roles project DUAL (`choice_duality`) and bystanders project DEFINED (`bystander_defined`). Item (2) substantially DONE. **SHIPPED** `otp_branch_merge.cure` (Idris `rel=same`). Open: n-ary label sets, the full mutual-compatibility of all three projections.
- [ ] Evolving-protocol inference (accepted set changes over a conversation) — composes B2 fixpoint + this solving

### B4. Full ordered / protocol selective receive (rest of G8)
- [x] PATTERN-DIRECTED selective receive (a pattern = a set of acceptable tags, generalizing the single-tag scan): `SelRecv(set, before, got, after)` with `selrecv_matches` (received message matches the pattern) + `selrecv_present` (it was actually in the mailbox) — **SHIPPED** `otp_pattern_receive.cure` (Idris `rel=same`). **DONE**: whole-conversation protocol-level ordering — `Std.Otp.Conversation`: `ConvRecv` runs a `Protocol` (sequence of expected tags) over a mailbox via per-step selective receive; `conv_order` proves the received conversation equals the protocol's tag sequence regardless of the mailbox's arrival order (a protocol-directed client always receives in protocol order). Idris `rel=same`

### B5. Signals semantics depth (KWC parity and beyond)
- [x] PER-SENDER signal ordering (KWC's pairwise guarantee): a mailbox is an `Interleave` of per-sender streams; `proj_left`/`proj_right` prove projecting to a sender recovers exactly that sender's stream in order — each sender's signals stay FIFO regardless of the interleaving — **SHIPPED** `otp_signal_order.cure` (Idris `rel=same`). Refines the single global mailbox-FIFO model to Erlang's actual pairwise ordering.
- [ ] link / monitor / exit modelled as SIGNALS with KWC ordering guarantees

### B6. Supervision depth
- [x] Restart INTENSITY bound (`max_restarts`) — `otp_restart_intensity.cure` (budget index; can't restart at zero). `max_seconds` window abstracted; bounded-run liveness theorem (`eventually_down`) now elaborates in-module (E6 unblocked). Sharpened by `eventually_down_len` (RESTART INTENSITY IS EXACT: `run_len(eventually_down(n)) = S(n)` — the canonical run tolerates EXACTLY `n+1` failures, `n` restarts then one shutdown, by induction on the budget via `snat_cong`), turning the qualitative "reaches Down" into a precise count. Idris `rel=same`
- [x] Differential restart-SET semantics `one_for_one` / `one_for_all` / `rest_for_one` tied to preservation (NVLang Def. 4.2) — all three strategies in `otp_supervisor.cure`, each preserving `Fleet(specs)`, differing only in which children they revive
- [x] Dynamic children / `simple_one_for_one`: `Pool(spec)` indexed by a SINGLE spec (not a list) certifies UNIFORM children; `start_child`/`terminate_child` vary the pool at runtime, `restart_pool` preserves spec + size — **SHIPPED** `otp_supervisor.cure` (Idris `rel=same`). The pool COUNT (not in the `Pool(spec)` index) now has a proved DYNAMIC-MEMBERSHIP ALGEBRA: `start_grows` (`pool_size(start_child(p)) = S(pool_size(p))` — grows by exactly one), `terminate_start_id` (`terminate_child(start_child(p)) = p` — the pool is a stack, push then pop cancels), and `restart_preserves_size` (`pool_size(restart_pool(p)) = pool_size(p)` — reviving every child changes liveness, not membership, by induction on the pool). So a `simple_one_for_one` supervisor's child count moves only by explicit start/terminate, never by a restart. Idris `rel=same`

### B7. Effects depth
- [x] Effect ALGEBRA + LAWS: OTP message ops carry no result, so effect programs form a MONOID under sequential composition (`seq` = append) — `seq_nil_l`/`seq_nil_r`/`seq_assoc` certify `(Eff, seq, ENil)` is a monoid, the reassociation/unit-simplification an effect handler relies on — **SHIPPED** `otp_eff_algebra.cure` (Idris `rel=same`). effect HANDLERS: a handler interprets the algebra and its correctness is a MONOID HOMOMORPHISM — `count_sends` (a send-counting handler into `(Nat,plus,0)`) + `count_hom` (`handle(a;b) = handle(a)+handle(b)`) — **SHIPPED** `otp_eff_algebra.cure` (Idris `rel=same`). Open: a value-returning free-monad `bind` (higher-order reduction in an index position, E10) + effect ROW/multiple-handler composition

### B8. Full gen_server / gen_statem lifecycle
- [x] Typed CALLBACK LIFECYCLE as a phase state machine — `Std.Otp.Lifecycle`: `Callback` = typed phase transition (init `PInit→PRunning`, handle_* keep `PRunning`, stop `→PTerminated`, terminate final); `Lifecycle` = well-formed callback sequence. Proves `init_only` (init is the only first callback), `nothing_reinits` (no callback returns to `PInit`), `terminated_absorbing` + `no_resurrection` (a terminated server stays terminated). **SHIPPED** `otp_lifecycle.cure` (Idris `rel=same`). Open: `code_change` version transitions; per-callback message/reply typing threaded through (the `Std.Otp.Proof`/`Std.Otp.Call` layer)
- [x] `gen_statem` EVENT POSTPONING: `SStep` (handle/postpone/redeliver) with per-move conservation of the unprocessed count — `handle_progresses` (only handling advances), `postpone_conserves`/`redeliver_conserves` (deferring/redelivering relocate an event, never drop it) — **SHIPPED** `otp_gen_statem.cure` (Idris `rel=same`). *No paper types postpone; this is gen_statem's defining feature.* Now LIFTED to whole RUNS: `SRun` (reflexive-transitive closure of `SStep`) with `srun_single`/`srun_trans` (reachability is a PREORDER — a move is a run, and runs compose end-to-end), and `handle_count_trans` (HANDLED-EVENT COUNT IS A MONOID HOMOMORPHISM: `handle_count(srun_trans(r1,r2)) = handle_count(r1) + handle_count(r2)` — events handled by concatenating two trajectories sum, so `SRun`-composition ↦ `Nat`-addition mapping `SRDone ↦ 0`; induction on `r1` via `plus_assoc`). Structural route sidesteps binding the indexed step's existential counts. FINDING: within-module `trans`/`sym` need an explicit `use Std.Equivalent` for CODEGEN (elaboration resolves them ambiently, but codegen emits `:unknown_global` without the import — `reflexive`/`rewrite` are fine without it). Open: full state-function callback protocol
- [x] `gen_event` EVENT MANAGER (`Std.Otp.GenEvent`, a new OTP behaviour): a `Manager(ifaces)` INDEXED by its handlers' interface list; `notify(e)` BROADCASTS to every handler (each processes via `step` if the event is in its interface, else ignores). The characteristic guarantee = a broadcast preserves the handler CONFIGURATION: `notify : Manager(ifaces) -> Manager(ifaces)` (type-level), plus explicit `notify_preserves_count` (handler count) and `notify_preserves_ifaces` (exact interface list) — so the manager's shape is stable under the event stream, only states advance. `add_handler` extends the interface index. The per-handler `step` helper keeps `notify` reducing STRUCTURALLY, sidestepping the E8 sequential-match gap. Plus DYNAMIC HANDLER MANAGEMENT: `remove_head : Manager(ICons(iface,rest)) -> Manager(rest)` (type-certifies the interface list SHRINKS), `add_remove_inverse` (`remove_head(add_handler(...)) = mgr` — install then remove restores the manager exactly, so a transient handler leaves no trace) and `count_after_add` (installing increments the count). Plus REMOVE-AT-POSITION (`Pos(ifaces)` witness `Here`/`There` + `delete_at` + `remove_at`): a DEPENDENTLY-TYPED list deletion — `remove_at(mgr, pos) : Manager(delete_at(pos))` removes exactly the handler at `pos` (all others, before and after, preserved in order), with the `Pos` witness guaranteeing the position exists so removal is total and can't drop the wrong handler. Recurses `match evidence(pos); match data(mgr)` (E1 sibling-refinement convoy). Plus SWAP/CODE_CHANGE (`swap_at` + `swap_preserves_ifaces`): replacing a handler's implementation (state) at `pos` keeps its interface slot — typed `Manager(ifaces) -> Manager(ifaces)` (same interface list, type-certified) with an explicit `swap_preserves_ifaces` confirming a code_change leaves the manager's configuration unchanged. **SHIPPED** `otp_gen_event.cure` (Idris `rel=same`). The event-manager behaviour is now complete: broadcast, install/remove, remove-at-position, swap/code_change.
- [x] `ETS` typed key/value table (`Std.Otp.Ets`, the BEAM storage primitive): a `set`-semantics table as an association list; `lookup`/`insert` proved a real finite map — `lookup_insert_eq` (LOOKUP-AFTER-INSERT: `lookup(k, insert(k,v,t)) = OSome(v)`) and `lookup_insert_neq` (a write to a DIFFERENT key is invisible: `key_eq(k,k2)=F ⟹ lookup(k, insert(k2,v,t)) = lookup(k,t)`, impossible same-key diagonals refuted via an `Empty` discriminator `t_ne_f`/`absurd`). NOTE: because `key_eq` is stuck on an abstract key, the K1 freeze keeps `lookup` FOLDED, so the laws are proved by CASE ANALYSIS on concrete keys (which makes `key_eq` compute) rather than `rewrite` — a real interaction of the K1 fix with decidable-equality reasoning. Plus `lookup_delete` (LOOKUP-AFTER-DELETE: `lookup(k, delete(k,t)) = ONone` — looking up a deleted key finds nothing; induction on the table casing on both keys so `key_eq` computes, at a matching head `delete` drops it, at a non-matching head `lookup` skips it, both continue to the deleted tail by IH). All three fundamental finite-map laws now certified. **SHIPPED** `otp_ets.cure` (Idris `rel=same`). Open: ordered_set semantics.
- [x] ETS `bag` / `duplicate_bag` MULTISET table (`Std.Otp.Bag`): distinct from the `set` table (overwrite) and the ordered map — a `duplicate_bag` KEEPS every insertion, so the meaningful observation is a key's MULTIPLICITY `count(k, t)`. The COUNT ALGEBRA is certified: `insert_incr` (INSERT INCREMENTS: `count(k, insert(k,t)) = S(count(k,t))` — a duplicate_bag adds an occurrence, never overwrites), `insert_other` (a DIFFERENT key's count is untouched: `keq(x,k)=MF ⟹ count(x, insert(k,t)) = count(x,t)`, same-key diagonals refuted by the premise `neq : MT = MF`), and `delete_insert` (DELETE-ONE UNDOES INSERT: `delete_one(k, insert(k,t)) = t` — `ets:delete_object` of a just-added occurrence returns the original bag; insert/delete_one are a push/pop pair). Finite key domain so `keq` reduces at proof time. Plus `count_delete_one` (DELETE-ONE DECREMENTS, saturating: `count(k, delete_one(k,t)) = mpred(count(k,t))` — removing one occurrence drops the count by one, clamped at zero if absent; induction on the bag, at a matching head the `S(...)` cancels the `mpred`, else the tail carries by IH), so with `insert_incr` the multiplicity is fully pinned under both ops (`+1` / `−1`-clamped). **SHIPPED** `otp_bag.cure` (Idris `rel=same`). Open: `ordered_set`.
- [x] Erlang `:queue` amortized TWO-LIST functional queue (`Std.Otp.Queue`, the FIFO primitive OTP uses everywhere): a `MkQ(front, back)` with logical sequence `front ++ reverse(back)`; the non-unique representation's correctness is that operations preserve the logical sequence. `enqueue_appends` (ENQUEUE APPENDS: `to_list(enqueue(x,q)) = to_list(q) ++ [x]` — pushing onto the reversed back lands `x` at the END of the logical order, via definitional `reverse(QCons(x,b)) = reverse(b) ++ [x]` + `append_assoc`), so the two-list encoding is a FAITHFUL FIFO. List ctors named `QNil`/`QCons` to avoid ambient list-ctor name collision when a using-module restates the type. Plus `dequeue` + `dequeue_reassembles` (DEQUEUE SPLITS OFF THE HEAD, losing nothing: `reassemble(dequeue(q)) = to_list(q)` in BOTH the fast non-empty-front case and the amortized front-empty back-REVERSED case — so the amortization is correct, reversing the back to refill the front preserves the FIFO sequence; reversal case reuses `deq_list_reassembles` + `append_nil_r`). TWO K1/ordering findings: (a) `dequeue`'s front-empty case must be LITERALLY `deq_list(reverse(b))` (not an inlined `match reverse(b)`) so it and the proof freeze to the SAME folded head; (b) `deq_list` must be defined BEFORE `dequeue` — certification is IN-ORDER, so a function calling a later-defined one isn't certified and won't δ-reduce in proofs. Plus REPRESENTATION INDEPENDENCE (`enqueue_respects`/`dequeue_respects`): the two-list split is not unique, and the operations are well-defined on the LOGICAL sequence — same `to_list` ⟹ `enqueue`/`dequeue` agree observably (enqueue via `enqueue_appends` + `append_cong`, dequeue via `dequeue_reassembles`). So the amortized queue faithfully implements the abstract FIFO; the internal split is invisible to a client. The two FIFO OBSERVATIONS are now proved too: `peek_dequeue` (PEEK AGREES WITH DEQUEUE: `peek(q) = head_of(dequeue(q))` — `peek` mirrors dequeue's front/back split so the fast case is reflexive and the front-empty case reduces to `peek_list(reverse(b)) = head_of(deq_list(reverse(b)))`), and `is_empty_reflects` (IS_EMPTY REFLECTS THE LOGICAL SEQUENCE: the O(1) both-lists-empty test `andb(qnull(f), qnull(b))` agrees with `qnull(to_list(q))` even for `{[], [a]}`-style states where the front is empty but the back is not — there both sides are `qnull(reverse(b)) = qnull(b)` via the reverse-invariance lemma `qnull_reverse`, whose cons case is discharged by `qnull_append_cons` since a `reverse` always ends in appending a singleton). **SHIPPED** (Idris `rel=same`). Open: ordered variants (priority queue).
- [x] Binary TREE backing `gb_trees`/`gb_sets` (`Std.Otp.Tree`): STRUCTURAL laws needing no key comparison — `mirror` (swap every node's left/right subtrees) is an INVOLUTION (`mirror_involution`: `mirror(mirror(t)) = t`, via `node_cong`) and mirroring PRESERVES the node count (`size_mirror`: `size(mirror(t)) = size(t)`, via `add_comm` since mirror swaps the two subtrees so their sizes add in the opposite order). Plus IN-ORDER FLATTEN + `flatten_mirror` (MIRROR REVERSES THE IN-ORDER TRAVERSAL: `flatten(mirror(t)) = reverse(flatten(t))` — reflecting the tree walks its nodes in reverse; the node case re-associates via `lrev_app` (reverse is an append-antihomomorphism) + `lapp_assoc`). FINDING: `trans`/`cong` chains fail on definitional reductions because implicit UNIFICATION does not δ-reduce (unlike full conv), so each such step needs an explicit reflexive reduction-bridge lemma (`lrev_cons`/`lapp_cons`) to make the chain's middle terms unify syntactically. Plus `size_flatten` (SIZE = TRAVERSAL LENGTH: `size(t) = length(flatten(t))` — `flatten` visits every node exactly once, so the structural count and the traversal length agree; node case via `llen_app` distributing length over `++` + `add_succ_r`). **SHIPPED** `otp_tree.cure` (Idris `rel=same`). The ORDERED (search-tree) side is now covered by the separate `Std.Otp.Bst` module below.
- [x] `:sets`/`:ordsets` set library (`Std.Otp.Set`): a set as a key list, `union` as concatenation; `union_member` proves the DEFINING law of set union — MEMBERSHIP DISTRIBUTES OVER UNION as boolean OR: `member(x, union(s1,s2)) = member(x,s1) or member(x,s2)` (an element is in the union exactly when in one part). Induction on `s1` with 3×3 concrete-key casing so `key_eq` computes (diagonal ⟹ both `T` via `orb(T,_)=T`, off-diagonal ⟹ IH). NB `bor` is a reserved bitwise-or token — the boolean OR is `orb`. **SHIPPED** `otp_set.cure` (Idris `rel=same`). The union algebra is now proved at the membership level: `orb_comm`/`orb_idem` (boolean OR is commutative and idempotent) lift through `union_member` into `union_comm_member` (`member(x, union(s1,s2)) = member(x, union(s2,s1))` — operand order is irrelevant) and `union_idem_member` (`member(x, union(s,s)) = member(x,s)` — uniting with self adds nothing), both via `trans`-chaining the distribution law on both sides. Associativity is now proved too: `orb_assoc` (with per-argument congruences `orb_cong_l`/`orb_cong_r`) lifts through `union_member` on both nestings into `union_assoc_member` (`member(x, union(union(s1,s2),s3)) = member(x, union(s1, union(s2,s3)))` — regrouping is irrelevant), so union is a commutative-idempotent-associative membership algebra. The `⊆`-order is now witnessed too: boolean implication `implb` with absorption lemmas `implb_orb_l`/`implb_orb_r` gives `union_upper_l`/`union_upper_r` (`x ∈ s1 ⟹ x ∈ union(s1,s2)` and `x ∈ s2 ⟹ x ∈ union(s1,s2)`) — `union` is the join of the membership semilattice. **SHIPPED** (Idris `rel=same`). `Std.Otp.Set` membership algebra COMPLETE (comm/idem/assoc/upper-bound).
- [x] ORDERED binary search tree backing `gb_trees`/`gb_sets` (`Std.Otp.Bst`): keys from a fixed finite ordered domain `KA < KB < KC` with a total `kcmp`, so the comparison REDUCES at proof time (same concrete-domain technique as `Std.Otp.Set`). `member` and `insert` both STEER BY `kcmp(k, v)` — go left on `KLt`, hit on `KEq`, go right on `KGt` — which is exactly what preserves the search invariant (left subtree < node < right). `insert_member` proves the DEFINING SEARCH-TREE LAW: `member(k, insert(k, t)) = T` — inserting a key makes a subsequent search for it succeed, whatever the tree shape. Because `member` and `insert` take the same branch at every node, casing `k` and each node key `v` (9-way) makes both reduce in lockstep: on a hit both stop at `T`, on `KLt`/`KGt` both descend the SAME subtree so the induction hypothesis carries. Plus `member_leaf` (empty tree has no members). FINDING: Idris rejected the first cut purely on PRELUDE NAME CLASHES — `Ordering`/`compare`/`LT`/`EQ`/`GT` are all in Idris's Prelude, so the transliteration must use clash-free names (`Cmp`/`kcmp`/`KLt`/`KEq`/`KGt`); this is a benign oracle-transliteration artifact, NOT a soundness divergence (Cure has no such clash). `insert_preserves` now proves INSERT PRESERVES EVERY OTHER MEMBER: `member(x,t)=T ⟹ member(x,insert(k,t))=T` — a search-tree insert never evicts a key already present. `insert` keeps the root key `v` at every node (rebuilding only one subtree), so `member(x, insert(k,t))` steers by the SAME `kcmp(x,v)` as `member(x,t)`: hit ⟹ `T`; else `x` descends its fixed side and either `k` went the same way (recurse — IH) or `k` touched the other side/root so that subtree is untouched (premise carries verbatim). 27-way casing on `x`/`v`/`k` makes every comparison reduce; the `Leaf` base is `member(x,Leaf)=F=T`, uninhabited (empty `match e` in Cure / `impossible` clause in Idris). **SHIPPED** `otp_bst.cure` (Idris `rel=same`). CAPSTONE — `insert_spec` proves the COMPLETE characterization: `member(x, insert(k, t)) = keq(x,k) or member(x, t)` — after inserting `k`, `x` is a member iff `x` IS `k` or `x` was already there. This SUBSUMES `insert_member` (`x = k`) and `insert_preserves` (`member(x,t) = T`) and also settles the last case (`x ≠ k`, `x ∉ t` ⟹ still absent). Same 27-way `x`/`v`/`k` casing: a hit ⟹ `T`; an off-side descent leaves that subtree and `keq(x,k)=F` so `orb` is the identity (reflexive); a same-side descent is the IH (defeq absorbs the `orb`). So membership-after-insert is fully pinned down. Plus `insert_comm` (INSERTS COMMUTE at the membership level: `member(x, insert(j, insert(k, t))) = member(x, insert(k, insert(j, t)))` — two keys inserted in either order give the SAME membership set though the trees differ structurally), derived ALGEBRAICALLY from `insert_spec` (each side unfolds to `keq(x,j) or (keq(x,k) or member(x,t))` via `orb_cong_r`, then the guards are swapped by `orb_swap`) — NO tree casing, a pure consequence of "insert adds exactly one key". `Std.Otp.Bst` search-tree insert theory COMPLETE. Open: in-order flatten sortedness, delete.
- [x] `gb_trees` AS A MAP (`Std.Otp.Gbt`): the ordered key→VALUE tree behind `gb_trees:enter`/`lookup` — distinct from `Std.Otp.Bst` (a set) in that every node carries a value, `insert` is `enter` (insert-or-UPDATE, replacing the value on a hit), and `lookup` returns an optional value. Keys are the fixed finite ordered domain `GA < GB < GC` with total `gcmp` (concrete-domain technique, so comparisons reduce at proof time). The two DEFINING FINITE-MAP LAWS are certified: `lookup_insert_eq` (LOOKUP AFTER ENTER RETURNS THE ENTERED VALUE: `lookup(k, insert(k, val, t)) = Some(val)` — `lookup`/`insert` steer by the same `gcmp(k,v)` so they descend together and `insert` writes `val` at exactly the node `lookup` reads; 9-way `k`/`v` casing, IH on the descended subtree) and `lookup_insert_neq` (ENTERING A DIFFERENT KEY DOESN'T DISTURB A LOOKUP: given `gkeq(x,k)=BF`, `lookup(x, insert(k, val, t)) = lookup(x, t)` — since `x≠k`, `x`'s search takes either the untouched side (reflexive) or the same side `k` did (IH); every same-key diagonal is refuted by the premise `neq : BT = BF` via empty `match neq` in Cure / a `btNeBf : BT=BF -> Void` void-clause in Idris; 27-way `x`/`v`/`k` casing). Distinctive ctor names (`GKey`/`GTree`/`GLeaf`/`GNode`/`GOpt`/`GNone`/`GSome`) so the module stays unambiguous when a using-module restates types alongside the other preloaded tree/opt modules. **SHIPPED** `otp_gbt.cure` (Idris `rel=same`). CAPSTONE — `lookup_spec` proves the COMPLETE conditional law: `lookup(x, insert(k, val, t)) = if x = k then Some(val) else lookup(x, t)` (via a `gsel` selector on `gkeq(x,k)`). One statement SUBSUMING both `lookup_insert_eq` (`x = k` ⟹ guard `BT`) and `lookup_insert_neq` (`x ≠ k` ⟹ guard `BF`), needing NO premise — the same-key case now RETURNS `Some(val)` instead of being refuted. Same 27-way casing: `x = v` hits the root (`Some(val)` if `k` also hit, else the untouched `Some(w)`), an off-side descent leaves that subtree with guard `BF` so `gsel` picks the plain lookup (reflexive), a same-side descent is the IH (defeq absorbs the `gsel`). `Std.Otp.Gbt` map lookup-after-enter theory COMPLETE. Open: delete, ordered fold.
- [x] THE DECIDABLE TOTAL ORDER ON KEYS (`Std.Otp.KeyOrder`): the ordering every ordered structure above STEERS BY but none of them certifies. `le` is a decidable relation on the finite domain `OA < OB < OC`, and the FOUR TOTAL-ORDER AXIOMS are proved — `le_refl` (`a ≤ a`), `le_total` (`a ≤ b or b ≤ a` = `OT`, any two keys comparable), `le_antisym` (`a ≤ b` and `b ≤ a` ⟹ `a = b`; off-diagonal one direction is `OF` so its assumed-`OT` premise is uninhabited), and `le_trans` (`a ≤ b`, `b ≤ c` ⟹ `a ≤ c`; 27-way casing, when `a ≤ c` holds it computes to `OT` else whichever hypothesis is actually `OF` refutes). So the search-tree and sorted-list invariants downstream rest on a CERTIFIED order, not an assumed one. Refutations via the uninhabited `OF = OT` (empty `match` in Cure / an `ofNeOt : OF=OT -> Void` void-clause in Idris). **SHIPPED** `otp_key_order.cure` (Idris `rel=same`). Plus `kmin`/`kmax` (MEET/JOIN = `gb_sets:smallest`/`largest`, the priority-queue ordering primitive): `kmin_comm`/`kmax_comm` (commutative), `kmin_idem`/`kmax_idem` (idempotent), `kmin_lower_l`/`kmin_lower_r` (meet is a LOWER bound of both operands — why `kmin` is the minimum a PQ extracts) + `kmax_upper_l`/`kmax_upper_r` (join is an UPPER bound), and `kmin_assoc` (meet is associative), so `(OKey, kmin)` is a bounded MEET-SEMILATTICE with `kmax` the dual join. Idris `rel=same`. Open: connect to `Bst`/`Gbt` comparison, `kmax_assoc`.
- [x] MIN PRIORITY QUEUE over the certified order (`Std.Otp.PQueue`): Erlang has no built-in PQ (built by hand from pairing heaps / `gb_sets` / sorted lists); this models the simplest correct one — an unsorted key bag with O(1) `insert` and the minimum computed by folding the certified meet `kmin`. The CORRECTNESS THEOREM is that the extracted `pq_min` is a genuine LOWER BOUND: `min_le_seed` (`pq_min(seed,q) ≤ seed` — induction on the queue, `kmin(k,m) ≤ m` by `kmin_lower_r` then `m ≤ seed` by IH, chained by TRANSITIVITY `le_trans`) and `insert_min` (`pq_min(seed, insert(k,q)) ≤ k` via `kmin_lower_l`). SIGNIFICANCE: the FIRST structure whose safety proof CONSUMES the order/semilattice axioms (`le_refl`/`le_trans`/`kmin_lower`) rather than re-establishing them — the lib module does this by cross-module `use Std.Otp.KeyOrder` (explicit-arg cross-module calls resolve cleanly). **SHIPPED** `otp_pqueue.cure` (Idris `rel=same`). Open: extract-min removes the minimum, `member`-level "≤ every element".
- [x] **SEARCH-TREE SEARCH SOUNDNESS** (`Std.Otp.BstSearch`, the HARD item — foundation for certified `delete`): on a WELL-FORMED binary search tree, binary search agrees with linear scan — `mem_eq_lmem` proves `is_bst(t)=OT ⟹ mem(x,t) = lmem(x,t)`, i.e. steering by the key comparison never MISSES an element a full scan finds (the search invariant is SOUND). EXTRINSIC formalization (plain `Tree` + boolean `is_bst`/`alllt`/`allgt`) after the INTRINSIC (GADT-indexed well-scoped BST) route hit a cluster of E-layer walls (see [[wellscoped-bst-elaborator-wall]]). Proof rests on the STRICT ORDER: `strict_trans` (+`strict_neq`/`strict_neq_r`) drive `below_not_lmem`/`above_not_lmem` (a key strictly below/above a bound is NOT linearly present in a subtree bounded that way), and the crux does 9-way `x`/`v` casing with the induction hypothesis on subtrees. ELABORATOR ROUTE-AROUNDS THAT MADE IT GREEN (all untrusted, NO TCB change): (a) `unfold_bst` return-position bridge to coerce `isbst(Node l v r)=OT` ↝ the `andb(…)` form (the `isbst(Node)→andb` ι-reduction does NOT fire in argument position — constructor-arg conversion gap); (b) explicit reflexive reduction-bridges `ml_*`/`ll_*` so the `trans`-chain endpoints are the reduced `mem`/`lmem` forms (`trans`/`sym` unification does not δ-reduce — same finding as the tree module's `lrev_cons`); (c) per-cell fact extraction INSIDE each `v`-branch (v concrete) — matching `v` refines `bst`'s type so the extraction type-checks, but the facts must be extracted where v is concrete, not hoisted above `match v`. Idris needs NONE of these (complete conversion) but DOES need refuting variables concrete (`void (ofNeOt p)` requires casing `y` so `cmp OC y` reduces). **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). CERTIFIED DELETE — `delete_bst` proves BST `delete` PRESERVES THE SEARCH INVARIANT: `is_bst(t)=OT ⟹ is_bst(delete(k,t))=OT`, INCLUDING the hard two-child merge/successor case. `delete` removes a node and (on a hit) MERGES its two subtrees; `merge_bst` proves the merge preserves the invariant across the deleted key's pivot — the crux being that the right subtree's lower bound must be pushed down to EACH left node, via `allgt_weaken`/`alllt_weaken` (a key strictly below/above a pivot stays below/above a weaker bound, by `strict_trans`). `delete_alllt`/`delete_allgt` prove delete preserves the node bounds; `andb_intro` assembles the three-way `is_bst` conjunction. So the order invariant is threaded through the GADT-free extrinsic tree at every node — the merge case the intrinsic route walled on. NOTE: the earlier `unify` delta-open fix (`cb2cbff1`) let `merge_bst`'s `isbst(Node)→andb` extraction work WITHOUT the `unfold_bst` bridge — the E-fix paying off in a real proof. **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). Plus DELETE REMOVES THE KEY — `mem_delete_eq` proves `is_bst(t)=OT ⟹ mem(k, delete(k,t)) = OF` (a deleted key is unfindable, whatever the tree). The hard case is `k = v` (delete = `merge(l,r)`): the chain is `mem(v, merge(l,r)) = lmem(v, merge(l,r))` (search soundness `mem_eq_lmem`, needing `is_bst(merge(l,r))` from `merge_bst`) `= lmem(v,l) or lmem(v,r)` (`lmem_merge`: merge preserves elements as a UNION, by `orb`-associativity) `= OF or OF = OF` (the deleted `v` is in NEITHER subtree: `alllt_absent`/`allgt_absent` — a key equal to a strict bound of a subtree is absent from it). So `delete` provably removes exactly its target, merge case included. Plus DELETE PRESERVES OTHER KEYS — `mem_delete_neq` proves `x≠k ⟹ mem(x, delete(k,t)) = mem(x,t)` (deleting one key leaves every other key's findability unchanged). Cleanly via the crux: reduces to a purely LINEAR `lmem_delete_neq` (needs NO `is_bst` — the `keq(x,k)=OF` premise cancels the removed node's `keq(x,v)` in the merge case, `lmem_merge` handles the union), sandwiched by `mem_eq_lmem` on both sides. So BST DELETE IS FULLY CERTIFIED: `delete_bst` (invariant preserved), `mem_delete_eq` (target removed), `mem_delete_neq` (others preserved) — the complete correctness spec, merge/successor case and all, threaded through the extrinsic order invariant. **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). Plus IN-ORDER FLATTEN SORTEDNESS — `flatten_sorted` proves `is_bst(t)=OT ⟹ sorted(flatten(t))=OT`: the in-order traversal of a BST is a STRICTLY SORTED list (the classic BST↔sorted-sequence correspondence). `flatten(Node(l,v,r)) = flatten(l) ++ v :: flatten(r)`; the crux `sorted_concat` glues two sorted halves across the pivot `v` (needs `all_lt_list(flatten l, v)`/`all_gt_list(flatten r, v)` from `flatten_all_lt_list`/`flatten_all_gt_list`, and `all_gt_list_weaken` to push a bound down each element by `strict_trans`). NOTE: unlike the search/delete proofs this needs NO 9-way key casing — there is no query key, only the tree/list structure — so it went through first try, a clean equational proof. `Std.Otp.BstSearch` is now the COMPLETE certified BST (search soundness + full delete correctness + flatten-sortedness). Item (1) of the hard-metatheory directive DONE. **SHIPPED** `otp_bst_search.cure` (Idris `rel=same`). Open (item 2): full N-role multiparty session coherence with branch-MERGE.

### B9. Out of scope (confirmed)
- Static DEADLOCK / LIVENESS — undecidable, a runtime result; a DDMon-style probe layer if ever wanted, orthogonal to the type system.

---

## "Beyond the papers" — the delta already achieved

| Property | Papers | Here |
|---|---|---|
| Operational preservation for mailbox-typed configs | Proof of Delivery DEFERS it | DONE (tag fragment), tied to inference |
| Mechanized | de'Liguoro–Padovani / NVLang = paper/sketch proofs | DONE, kernel-certified + Idris cross-checked |
| Dependent + LINEAR reply (`ReplyOf`, exactly-once) | none | DONE, intrinsic + operational |
| Progress + multi-pid interleaving | NVLang single-actor, no progress | DONE |
| Timers, call-totality, effect-honesty | no paper | DONE |
| Inference adequacy (infer ⇒ operational safety) | literature only STATES it | DONE, sequential + branching |
| Recursion fixpoint inference (`infer(BRec)` = least fixed point, operationally safe) | literature STATES the lfp exists; leaves coverage/adequacy for recursion open | DONE — syntax-derived transfer, finite Kleene stabilization, coverage, adequacy, principality, all kernel-certified + Idris cross-checked (finite universe) |

## Critical path to "FULL"
B2 (recursion fixpoint) is **COMPLETE** (transfer derivation → monotonicity → finite Kleene
stabilization → coverage → operational adequacy → principality; `otp_finite_fixpoint` /
`otp_interface_bridge` / `otp_recursive_transfer` / `otp_recursive_adequacy` / `otp_recursive_run`),
for the finite (bounded-tag) universe. B3 (counting fragment) is the largest remaining piece and is
partly EXTERNAL (Presburger/Z3). B5–B8 are depth/fidelity extensions. B1 is cleanup + ergonomics.
Everything in B1/B2/B4/B5/B6/B7/B8 is in-Cure provable; only B3's solver step sits outside the TCB.
