mod Dp02
  type Nat = Z | S(Nat)
  type MyEq(a: Type) indices (x: a, y: a)
    mrefl : MyEq(a, w, w)
  # TODO: rewrite the forced index as a dot pattern after Task 5 lands (.e syntax).
  fn congS({a: Nat}, {b: Nat}, p: MyEq(Nat, a, b)) -> MyEq(Nat, S(a), S(b)) = match p
    mrefl() -> mrefl()
end
